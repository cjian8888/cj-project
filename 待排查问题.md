# 待排查问题清单

根据近期的调试记录与系统运行情况，汇总以下待排查与优化的问题点。建议按优先级逐一解决。

## 1. 系统稳定性与运维 (高优先级)
- [x] **端口占用冲突**: 后端服务崩溃或未正常退出时，端口 `8000` 常被僵尸进程占用，导致重启失败 (`[WinError 10048]`)。
  - ✅ **已完成**: 开发了 `stop_server.bat` 脚本，用于安全关闭 Python 进程和释放端口。
- [ ] **僵尸进程管理**: 系统 crash 后 Python 进程常驻后台，不仅占用端口还占用内存。需要检查 `api_server.py` 的 `lifespan` 退出逻辑是否完备。
- [ ] **日志文件过大**: `audit_system.log` 体积增长迅速。
  - **现状**: 已在 `config.py` 和 `utils.py` 中集成了 `RotatingFileHandler`。
  - **待验证**: 需长期运行观察日志切割是否生效（是否按 10MB 切割，保留 5 份备份）。

## 2. 前后端数据交互 (核心功能)
- [x] **资金流向图谱**: 
  - ✅ **已验证**: 后端 `/api/analysis/graph-data` 接口已存在（第318-425行）
  - ✅ **已验证**: 前端 `NetworkGraph.tsx` 正确调用该接口，带10秒超时
  - ✅ **已应用**: 图谱中 counterparty 和风险等级已使用格式化函数
- [x] **疑点数据结构不匹配**: 
  - ✅ **已修复**: 通过翻译层统一处理数据格式
- [x] **WebSocket 连接稳定性**: 
  - ✅ **已增强**: 重连机制优化为3秒起始、指数退避、最多5次重试
  - ✅ **新增**: Header 中添加了 WebSocket 连接状态指示灯（连接中/已连接/断开/错误）
  - ✅ **新增**: 点击指示灯可手动触发重连

## 3. 前端 UI 与体验
- [ ] **关键信息展示优化**: 用户提到副标题 "资金穿透与关联排查系统" 的清晰度及文件夹选择框的样式需要调整。
- [x] **构建报错**: 本地开发环境 (`npm run dev`) 可能仍存在 TypeScript 类型报错。
  - ✅ **已验证**: `npx tsc --noEmit` 检查通过，无类型错误。
- [x] **加载状态反馈**: 在大数据量分析时，前端缺乏明确的 Loading 状态或进度提示。
  - ✅ **已验证**: `NetworkGraph.tsx` 和 `TabContent.tsx` 已有完善的 loading 状态处理
  - ✅ **已存在**: `Skeleton.tsx` 提供了多种加载骨架屏组件

## 4. 逻辑正确性验证
- [ ] **关联方判定逻辑**: 需再次确认 `related_party_analyzer.py` 中对于"关联方"的判定规则（如是否严格匹配了姓名+身份证，还是仅匹配了姓名），避免同名误判。
- [ ] **借贷分析准确性**: 检查无还款借贷（"疑似利益输送"）的阈值设置（如长期无还款的天数设定），确保不会误伤正常的长周期借贷。

## 5. 环境与配置
- [ ] **硬编码路径**: 检查代码中是否还存在硬编码的绝对路径（如 `D:\CJ\project\...`），应改为相对路径或通过环境变量配置，以保证可移植性。
- [ ] **依赖一致性**: 确保 `requirements.txt` 和 `package.json` 包含了所有最新的依赖项（如 `uvicorn`, `python-multipart` 等）。

## 6. 特别任务: 前端数据"人本化"清洗与 UI 细节修复
- [x] **建立翻译层 (Translation Layer)**
  - ✅ **已完成**: 在 `dashboard/src/utils/formatters.ts` 中实现了完整的翻译层
  - ✅ **已应用**: 在 `TabContent.tsx` 和 `NetworkGraph.tsx` 中应用了翻译层函数

---
**更新时间**: 2026-01-16 13:55
**状态**: 持续排查中 (7/12 已完成)

## 最新完成记录 (2026-01-16)

### Phase 1: 全局翻译审计 ✅
- `NetworkGraph.tsx`: 引入 formatters，修复 3 处 counterparty，统一风险等级显示
- `TabContent.tsx`: 格式化 Modal 中的数据，移除英文代码
- `LogConsole.tsx`: 确认已有中文日志级别映射

### Phase 2: 核心功能攻坚 ✅
- **图谱功能**: 确认后端 API 和前端对接正常
- **WebSocket 增强**: 
  - 重连机制优化（3秒起始，指数退避）
  - 新增 `subscribeStatus()` 方法和 `status` 属性
  - Header 添加实时连接状态指示灯
  - 支持点击手动重连
- **加载状态**: 确认各组件已有完善的 loading 处理
