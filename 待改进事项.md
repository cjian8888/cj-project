# 待改进事项 (Remaining Improvements)

> 最后更新：2026-01-18
> 状态：P1、P2 已完成

---

## ✅ P1 - 硬编码阈值迁移（已完成）

### 完成情况
在 `config.py` 中添加了 6 个新阈值常量，并更新了以下模块中的硬编码值：

| 文件 | 新配置名 |
|------|----------|
| loan_analyzer.py | `LOAN_MIN_MATCH_AMOUNT` |
| time_series_analyzer.py | `TIME_SERIES_HIGH_RISK_AMOUNT`, `SUDDEN_CHANGE_MIN_AMOUNT` |
| fund_penetration.py | `FUND_FLOW_MIN_AMOUNT`, `GRAPH_EDGE_MIN_AMOUNT` |
| suspicion_detector.py | `SUSPICION_MEDIUM_HIGH_AMOUNT` |
| financial_profiler.py | 使用 `HIGH_FREQUENCY_SALARY_CAP` |

> 注意：`income_analyzer.py` 中的 `income > 100000` 和 `amount >= 500000` 已在 config.py 中有对应配置 `INCOME_VERY_LARGE_MIN` 等，无需新增。

---

## ✅ P2 - TODO 项完成（已完成）

更新了 `suspicion_detector.py` 中的 TODO 注释，添加了预留模块说明和参考实现指引。

---

## ✅ 已完成项

- [x] 修复 6 处裸 except 子句
  - incremental_analyzer.py (L41, L145, L201)
  - risk_scoring.py (L190)
  - bank_formats.py (L198)
  - report_generator.py (L124)

- [x] 为所有 TXT 报告添加专业头部说明

- [x] 移除报告数据截断限制

- [x] 修复 nan 值显示问题

- [x] **P1 硬编码阈值迁移** (2026-01-18)

- [x] **P2 TODO 项完成** (2026-01-18)

---

## 快速验证命令

```bash
# 运行所有测试
./venv/bin/python -m pytest tests/ -q

# 检查是否还有裸 except
grep -rn "except:" *.py | grep -v "except (.*)" | grep -v "test"

# 检查硬编码阈值
grep -rn "> 5000\|> 10000\|>= 50000\|>= 100000" *.py | grep -v config
```
