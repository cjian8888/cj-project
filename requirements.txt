# 资金穿透与关联排查系统 - 依赖清单
# Python >= 3.8

# 核心数据处理
pandas>=1.3.0
numpy>=1.20.0
openpyxl>=3.0.0

# 文档处理
pdfplumber>=0.7.0
python-docx>=0.8.0

# 模板引擎（可选，用于高级模板）
jinja2>=3.0.0

# Excel格式化（可选，用于更好的Excel输出）
xlsxwriter>=3.0.0

# 注意：以下为Python标准库，无需安装
# - datetime, re, json, os, glob, collections, typing
