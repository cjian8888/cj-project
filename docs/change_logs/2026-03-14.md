# 2026-03-14 变更记录

## 主题

建立 Windows 单机离线交付主链路。

## 本次完成

- `api_server.py`
  - 新增 `GET /dashboard/` 及其子路径的静态产物承载能力
  - 支持 SPA 路由回落到 `dashboard/dist/index.html`
  - 启动时主动确保 `data/`、`output/` 目录存在
  - 根接口返回新增 `dashboardUrl` 与 `deliveryTarget`
- `dashboard/src/services/api.ts`
  - 默认 API 改为同源访问
  - 默认 WebSocket 改为基于当前页面地址推导
- `dashboard/vite.config.ts`
  - 生产构建 `base` 改为 `/dashboard/`
  - 开发态新增 `/ws` 代理
- `start_backend.bat`
  - 改为基于脚本所在目录启动，移除硬编码路径
- `start_frontend.bat`
  - 改为基于脚本所在目录启动，移除硬编码路径
- 新增 Windows 打包入口
  - `build_windows_package.py`
  - `build_windows_package.bat`
  - `fpas_windows.spec`
  - `requirements-windows-build.txt`
- 新增交付基线文档
  - `WINDOWS_OFFLINE_DELIVERY.md`

## 设计结论

- 继续保留当前 Mac/Windows 双端开发模式
- 但从现在开始，所有新增代码都必须服从最终交付形态：
  - Windows
  - 单机
  - 离线
  - one-folder 包

## 后续建议

1. 在 Windows 真机执行 `python build_windows_package.py`
2. 验证 `dist/fpas-offline/fpas.exe` 能否直接承载 `/dashboard/`
3. 若通过，再补充最终交付启动器与交付说明
