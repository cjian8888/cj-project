# 2026-03-15 工作日志

## 主题

AML / 资金穿透主线改造收口，补齐分析日志固化、报告链路修正、前后端路径同步与缓存恢复，并完成一轮真实前端回归。

## 今日完成

### 1. AML / 资金穿透主线改造继续收口

- 围绕 AML、资金穿透、关联方、线索聚合、统一风险模型继续完成本轮主线改造
- 同步调整多类提取器、分析器、检测器与报告构建逻辑，统一输出口径与 explainability 展示
- 重点覆盖：
  - `aml_analyzer.py`
  - `fund_penetration.py`
  - `related_party_analyzer.py`
  - `clue_aggregator.py`
  - `unified_risk_model.py`
  - `investigation_report_builder.py`
  - `specialized_reports.py`

### 2. 分析执行日志固化能力落地

- 在 `api_server.py` 中新增并稳定化分析运行日志固化机制
- 当前分析完成后会保留三类日志产物：
  - `analysis_results/分析执行日志.txt`
  - `analysis_logs/analysis_run_*.log`
  - `analysis_logs/analysis_run_latest.log`
- 日志固化在分析收尾时停止继续写入，避免后续无关日志污染本次分析记录

### 3. 报告生成链路修正

- 调整分析阶段收尾顺序，先保存完整缓存，再生成 txt / HTML / 专项报告及目录清单
- `save-html` 后补刷新 `报告目录清单.txt`
- 修正专项报告若干展示问题：
  - 区分“同主体现金时序伴随”与“跨实体现金碰撞”
  - 补入节假日敏感交易
  - 避免 AML 零计数场景误写“未发现”

### 4. 前后端路径同步与缓存自动恢复修复

- 核心问题：前端切换输出目录后，仅更新前端状态和 `localStorage`，后端 `active output` 未同步，导致：
  - 页面刷新后不自动恢复缓存
  - 图谱接口返回“分析尚未完成”
- 本次修复内容：
  - 后端新增活动路径同步接口
  - 同步后按当前输出目录检查并恢复缓存
  - `status/results/graph/audit-navigation` 改为基于当前活动输出目录自愈恢复
  - 前端初始化时改为“先同步路径，再恢复缓存”
  - 前端在“选择目录完成”和“输入框失焦”时同步路径到后端

### 5. Safari 报告预览兼容修复

- 修复 Safari 对 `/api/reports/preview/{filename}` 发起 `HEAD` 请求时返回 `405` 的问题
- 现在报告预览接口同时支持 `GET` / `HEAD`
- 保留原有预览行为，不改前端，不引入新依赖

### 6. 节假日服务兼容修复

- 排查真实分析日志中的节假日告警，确认问题来自 `chinese-calendar` 不同版本返回值结构不一致
- 在 `holiday_service.py` 中补充节日名称兼容解析：
  - 兼容字符串返回
  - 兼容对象带 `.chinese` 属性的返回
  - 统一英文节日名到中文节日名映射
- 消除 `"'str' object has no attribute 'chinese'"` 这类误报，保留本地配置兜底能力

### 7. 同行人 / 同住宿 / 同住址链路专项调查与修复

- 对“同行人分析”“同住宿分析”“同住址分析”做了按数据流的全链条排查
- 明确链路来源：
  - `住宿记录` 来自 `hotelData`
  - `同行资金碰撞` 来自 `derived_data.correlation.travel_companions`
  - `同住宿资金碰撞` 来自 `derived_data.correlation.hotel_cohabitants`
  - `同住址 / 同车违章` 来自 `coaddressData` / `coviolationData`
- 修复与增强点：
  - `api_server.py` / `cache_manager.py` 补齐 P2 外部缓存的保存与回放
  - `multi_source_correlator.py` 修复同住址/同住宿资金碰撞的匹配逻辑与阶段容错
  - `investigation_report_builder.py` 接入 `coaddress` / `coviolation` / `hotel` / `flight` / `railway` 缓存读取与报告落表
- 新增 `tests/test_multi_source_correlator.py`，并补强 `tests/test_api_server_config_flow.py`

### 8. 报告全量复核与 HTML 展示修复

- 对 HTML 报告执行一轮“缓存 -> builder -> 最终 HTML” 的穿透式复核
- 批量对账结果：
  - 覆盖 18 名个人
  - 检查 234 个关键口径点
  - `0` 个 builder 级 mismatch
- 样例穿透复核：
  - `高杰`
  - `宋耀先`
  - `张俊`
  - `薛华菁`
  - `王磊`
- 修复本轮确认的展示问题：
  - 去除房产描述中的 `平方米平方米`
  - 统一 HTML 日期展示，清理 `T00:00:00` / `YYYYMMDD` 原始串
  - 收紧“高频/多人同行对象”筛选逻辑，避免单次且非多人同行误入
- 当前保留的显式提示：
  - `日期待核`
  - `登记时间待补充`
  这些属于源数据缺失提示，不再误判为展示 bug

## 真实验证结果

### 已完成验证

- `pytest -q tests/` = `551 passed, 2 warnings`
- `pytest -q tests/test_api_server_config_flow.py` = `13 passed, 2 warnings`
- `cd dashboard && npm run type-check` = `passed`
- `cd dashboard && npm run build` = `passed`
- `pytest -q tests/test_investigation_report_builder_metrics.py -q` = `passed`
- 重新生成 `output/analysis_results/初查报告.html` 并复核：
  - `平方米平方米` = `0` 命中
  - `T00:00:00` = `0` 命中
  - `20240105` 这类未格式化日期 = `0` 命中

### Safari 真实前端回归

- 使用 Safari WebDriver 对后端承载的 `http://127.0.0.1:8000/dashboard/` 进行真实验证
- 复核点：
  - 页面初始化后恢复用户路径
  - 选定输出目录后自动同步后端
  - 若输出目录存在缓存，则自动恢复概览数据
  - 图谱页不再出现“分析尚未完成 / 暂无图谱数据”
  - 报告页可正常打开，不再因 `HEAD` 探测产生 `405`
- 验证结果：
  - 前端确实按用户所选输出目录恢复缓存，而非误读默认 `output/`
  - 图谱成功恢复并渲染
  - 报告列表与报告页可正常进入

## 本次新增 / 重点更新文件

- 后端主入口：
  - `api_server.py`
- 前端路径同步与恢复：
  - `dashboard/src/contexts/AppContext.tsx`
  - `dashboard/src/components/Sidebar.tsx`
  - `dashboard/src/services/api.ts`
- 报告与专项报告：
  - `investigation_report_builder.py`
  - `specialized_reports.py`
- 风险与聚合相关：
  - `clue_aggregator.py`
  - `risk_scoring.py`
  - `unified_risk_model.py`
- 测试：
  - `tests/test_api_server_config_flow.py`
  - `tests/test_holiday_detection_flow.py`
  - `tests/test_investigation_report_builder_metrics.py`
  - `tests/test_multi_source_correlator.py`
  - 以及本轮补充/更新的 AML、report、aggregator、risk 相关测试文件

## 已知非阻断项

- `dashboard lint` 仍有大量历史问题，属于旧债，本轮未作为阻断项处理
- `schemas/config.py` 仍存在 2 条 Pydantic 2.12 deprecated warning，后续再统一处理

## 交接建议

1. 继续以“后端承载前端生产构建”作为唯一交付主路径，避免重新依赖 Vite 开发服务器。
2. 后续若再涉及“路径切换 / 缓存恢复 / 图谱联动”，必须同时验证：
   - 前端显示路径
   - 后端 active output
   - `/api/status`
   - `/api/results`
   - `/api/analysis/graph-data`
3. 若继续推进 Windows 单机离线交付，建议优先在真机上跑一轮完整 one-folder 验证。

## 晚间补充修复

### 1. 资产与家庭单元口径再次收口

- 修复 `asset_extractor.py`，精准房产不再只扫描首个“自然资源部精准查询（定向查询）”目录，而是遍历全部匹配目录
- 修复 `api_server.py`，家庭汇总在调用 `family_finance.calculate_family_summary()` 时补入房产与车辆资产，避免 `all_family_summaries` / Excel 底稿房产数失真
- 增加“仅保留有画像交集家庭单元”的保护，避免 `family_units_v2` 中的幽灵家庭再次污染专项报告、HTML 与主报告口径

### 2. 报告构建链防回退加固

- 在 `investigation_report_builder.py` 中新增统一的“有效家庭单元”入口
- 报告构建、家庭成员关系、家庭 roster、家庭 fallback 分组等关键路径，改为统一走过滤后的有效家庭单元，而不再在多处直接裸读 `family_units_v2`
- 目标是彻底封死“19 个原始家庭单元重新污染 18 个有效分析单元”的回退入口

### 3. 行为特征报告真实溯源修复

- 修复 `behavioral_profiler.py`，行为特征检测结果补齐真实 `source_file / source_row_index`
- 修复 `specialized_reports.py`，即使当前缓存里还是旧版 `income_row_idx / expense_row_idx`，生成行为特征报告时也会回到 `output/cleaned_data` 自动补齐真实源文件与原始 Excel 物理行号
- 当前 `行为特征分析报告.txt` 已可展示：
  - `收入溯源`
  - `收入行号`
  - `支出溯源`
  - `支出行号`

### 4. 针对性回归验证

- `pytest -q tests/test_behavioral_profiler.py tests/test_specialized_reports.py tests/test_investigation_report_builder_metrics.py tests/test_api_server_config_flow.py`
  - `77 passed`
- 基于现有缓存重生成报告后复核：
  - `初查报告.html` 分析单元数仍为 `18`
  - `资产全貌分析报告.txt` 仍为 `18` 个家庭、`25` 套房产
  - `核查结果分析报告.txt` 仍为 `18` 名核心人员、`199379` 条流水
  - `行为特征分析报告.txt` 已出现真实双向溯源字段

## 深夜补充：电子钱包补充层接入与风险增强

### 1. 电子钱包数据正式接入主分析链路

- 新增 `wallet_data_extractor.py`，对支付宝注册信息、支付宝账户明细、微信注册信息、微信登录轨迹、财付通注册信息、财付通交易进行递归扫描与统一抽取
- 输入目录规则明确为“任意输入根目录 + 电子钱包专用子目录”，推荐路径：
  - `补充数据/电子钱包/批次_YYYYMMDD/`
- 主链扫描器对电子钱包目录做豁免，避免将微信 / 支付宝 / 财付通样本误并入银行 `cleaned_data`
- 电子钱包数据以补充层方式进入：
  - `analysis_cache/walletData.json`
  - `profiles[*].wallet_summary`
  - `suspicions.wallet_alerts`
- 支持多个电子钱包子目录自动合并扫描，不要求单目录唯一

### 2. 正式专项产物落地

- 新增 `wallet_report_builder.py`
- 分析完成后自动在 `output/analysis_results/` 生成三类正式产物：
  - `电子钱包补充分析报告.txt`
  - `电子钱包补充清洗表.xlsx`
  - `电子钱包重点核查清单.txt`
- 其中 Excel 采用“来源文件 / 支付宝注册 / 支付宝明细 / 微信注册 / 微信登录 / 财付通注册 / 财付通交易 / 主体摘要 / 风险预警 / 未归并微信账号”多工作表结构，便于独立清洗与人工复核

### 3. 前端补充页与图谱侧接入

- 新增前端补充页组件 `dashboard/src/components/WalletSupplementTab.tsx`
- 当前前端已支持：
  - 展示电子钱包主体摘要、文件来源统计、未归并微信账号
  - 展示高风险电子钱包预警
  - 手工导出“前端精简版重点核查清单”
- 图谱页补入电子钱包对手方节点、电子钱包关系边、电子钱包预警区块和电子钱包对手方区块
- KPI、风险情报筛选、前端类型定义、缓存恢复链路同步纳入电子钱包字段

### 4. 聚合评分、统一风险模型与报告链路补强

- `clue_aggregator.py` 新增电子钱包主体摘要与电子钱包预警聚合能力
- 聚合层优先消费预警源头提供的：
  - `risk_score`
  - `confidence`
  - `rule_code`
  - `evidence_summary`
- `unified_risk_model.py` 将 `wallet_summaries` / `wallet_alerts` 纳入综合风险评分与 explainability
- 正式报告与 HTML 个人章节新增“电子钱包补充核查”内容

### 5. 风险判断增强

- 在主体摘要型规则之外，新增 `wallet_risk_analyzer.py` 作为第二层增强模块
- 当前已落地的高级规则包括：
  - `wallet_bank_counterparty_overlap`
  - `wallet_bank_quick_outflow`
  - `wallet_split_collection`
  - `wallet_quick_pass_through`
  - `wallet_night_activity`
- 主流程接入顺序为：
  1. Phase 4 提取电子钱包补充数据
  2. 主体映射完成后执行 `reconcile_wallet_data()`
  3. 再执行 `wallet_risk_analyzer.enhance_wallet_alerts()`
- 保持原则：
  - 电子钱包只增强补充层，不回写银行主清洗链
  - 不影响前序银行分析结论

### 6. 文档与架构说明补充

- `README.md` 补充电子钱包目录规则、自动合并扫描策略、正式产物说明
- 新增专项说明文档：
  - `docs/电子钱包补充数据接入方案.md`
- `docs/architecture_overview.md` 补入电子钱包补充层在数据流中的位置与输出边界

### 7. 针对性验证结果

- `pytest tests/test_wallet_risk_analyzer.py tests/test_wallet_data_extractor.py tests/test_clue_aggregator.py -q`
  - `13 passed`
- `pytest tests/test_api_server_config_flow.py -q`
  - `19 passed`
- `python3 -m py_compile wallet_risk_analyzer.py wallet_data_extractor.py api_server.py`
  - 通过
- `cd dashboard && npm run type-check`
  - 通过

### 8. 本轮新增 / 重点更新文件

- 新增：
  - `wallet_data_extractor.py`
  - `wallet_report_builder.py`
  - `wallet_risk_analyzer.py`
  - `dashboard/src/components/WalletSupplementTab.tsx`
  - `tests/test_wallet_data_extractor.py`
  - `tests/test_wallet_report_builder.py`
  - `tests/test_wallet_risk_analyzer.py`
  - `docs/电子钱包补充数据接入方案.md`
- 重点更新：
  - `api_server.py`
  - `cache_manager.py`
  - `clue_aggregator.py`
  - `unified_risk_model.py`
  - `investigation_report_builder.py`
  - `dashboard/src/types/index.ts`
  - `dashboard/src/services/api.ts`
  - `dashboard/src/components/TabContent.tsx`
  - `dashboard/src/components/NetworkGraph.tsx`
  - `templates/report_v3/person_section.html`
  - `templates/report_v3/report.html`
