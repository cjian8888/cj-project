# 2026-03-08 变更记录

## 变更目标
- 提升风险情报识别的准确性与可解释性。
- 修复 HTML 报告数据缺失与模块开关不生效问题。
- 打通前端报告配置（核查对象/阈值/模块）到后端生成算法。
- 修复数据概览「风险研判」金额统计为 0 的前端展示问题。

## 核心改动摘要
- 后端分析链路增强：补齐资金穿透、ML 分析、线索聚合入参，优化疑点检测主流程与兼容字段。
- 现金碰撞规则优化：默认不将“同一主体自碰撞”作为主风险碰撞输出，降低误报。
- 报告生成增强：支持运行时 `thresholds/sections`，并真正作用于大额转账/现金分析和 HTML 模板显示。
- 前端适配：统一 API 地址来源，状态字段兼容，KPI 风险金额聚合逻辑修复。
- 报告模板修复：`report_v3` 按模块开关动态显示 summary/assets/risks，避免无效配置。

## 影响文件
- `api_server.py`
- `clue_aggregator.py`
- `config/report_phrases.yaml`
- `dashboard/src/components/KPICards.tsx`
- `dashboard/src/components/PrimaryTargetsConfig.tsx`
- `dashboard/src/components/ReportBuilder.tsx`
- `dashboard/src/components/TabContent.tsx`
- `dashboard/src/contexts/AppContext.tsx`
- `dashboard/src/services/api.ts`
- `detectors/cash_collision_detector.py`
- `detectors/direct_transfer_detector.py`
- `detectors/time_anomaly_detector.py`
- `investigation_report_builder.py`
- `loan_analyzer.py`
- `risk_scoring.py`
- `specialized_reports.py`
- `suspicion_detector.py`
- `suspicion_engine.py`
- `templates/report_v3/person_section.html`
- `templates/report_v3/report.html`
- `time_series_analyzer.py`
- `unified_risk_model.py`
- `wealth_product_extractor.py`

## 变更规模
- 23 个文件修改
- 2572 行新增，959 行删除（以提交前 `git diff --stat` 为准）

## 晚间补充（审计一致性修复）
- 修复清洗口径一致性问题：
  - 去重键改为“方向(I/E/M)+金额”，避免收入与支出同金额互相误去重。
  - 收入/支出字段增加负值归一化，统一为非负语义，确保后续汇总口径一致。
  - 公司口径清洗增强（公司主体识别、对公账户分类、对公账号银行卡识别），并在清洗入口传递实体名。
- 修复报告链路兼容性问题：
  - `investigation_report_builder.py` 的 `get_total_inflow/get_total_outflow` 兼容 `snake_case/camelCase` 字段。
  - 修复统一五维分析中错误引用 `DimensionScore` 导致的潜在运行异常（改为 `LegacyDimensionScore`）。
- 清理并校验 `income_analyzer.py` 导入，移除重复污染导入。

## 晚间验证结果
- 单测：`pytest -q tests/test_data_cleaner.py` 通过（29 passed）。
- 编译检查：`python3 -m py_compile income_analyzer.py investigation_report_builder.py data_cleaner.py tests/test_data_cleaner.py` 通过。
- 原始数据重跑对比（不覆盖旧结果）：
  - 对比目录：`output/cleaned_data_compare_20260308_215307/`
  - 总体变化：行数 `+1757`，收入 `+21,571,557.29`，支出 `+8,725,158.57`。
  - 负值清零：负收入 `1327 -> 0`，负支出 `980 -> 0`。
