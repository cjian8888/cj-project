# HTML报告盲审记录 2026-03-22

## 目的

本记录用于沉淀 2026-03-22 针对 `output/analysis_results/初查报告.html` 开展的人工盲审结果。

约束如下：

- 不使用现有程序的聚合、清洗、报告生成逻辑
- 只回查原始源文件
- 重点验证 HTML 展示层是否与原始证据一致

---

## 样本一：朱永平

### 抽样对象

- 对象：朱永平
- HTML 章节位置：`初查报告.html` 第三部分朱永平家庭，个人章节起始于 `L5865`

### 已直接核实一致的字段

- 房产 `2` 套，面积合计 `219.35㎡`
- 航班记录 `56` 条
- 住宿记录 `70` 条
- 同住址 `4` 人
- 同车违章 `1` 条
- 企业登记 `1` 家：`南通优逸克新材料科技有限公司`
- 企业登记状态：`注销`
- 电子钱包：
  - 支付宝 `190` 笔
  - 财付通 `470` 笔
  - 微信账号 / 财付通账号 `1 / 2`
  - 微信登录轨迹 `973` 条
  - 最近登录时间 `2026-02-05 13:06:23`
  - 钱包累计规模 `5.0509` 万元

### 已直接核实不一致的字段

#### 1. 银行原始总流入 / 总流出

- HTML：
  - 总流入 `327.62` 万元
  - 总流出 `454.61` 万元
- 原始银行流水 2022-2025 四家银行直接汇总：
  - 总流入 `367.9143` 万元
  - 总流出 `494.6114` 万元

结论：

- HTML 展示的“原始资金流入 / 流出”不是原始银行流水直接求和结果

#### 2. 大额收入 / 大额支出

- HTML：
  - 大额收入 `76` 笔 `285.33` 万元
  - 大额支出 `41` 笔 `371.18` 万元
- 原始银行流水按单笔 `>= 1 万元` 直接复算：
  - 大额收入 `77` 笔 `325.3343` 万元
  - 大额支出 `42` 笔 `411.1755` 万元

结论：

- HTML 大额统计口径被人为收缩，且与“原始流水”表述不一致

#### 3. 现金收支

- HTML：
  - 现金流入 `32.68` 万元
  - 未见现金支出
- 原始银行流水人工口径复算：
  - 现金存入 `38.76` 万元
  - 现金取现 `28.10` 万元

结论：

- HTML 现金段明显失真，至少漏掉了大量 ATM 取现

#### 4. 车辆登记时间

- HTML：
  - `登记时间待补充`
- 原始机动车文件：
  - `初次登记日期 = 2020-01-10`

结论：

- 车辆展示字段缺失

#### 5. 铁路记录口径

- HTML：
  - 铁路记录 `195` 条
- 原始铁路文件：
  - `票面信息 112` 条
  - `交易信息 83` 条

结论：

- HTML 的 `195` 实际是两个不同 sheet 的行数合并，不是纯粹“乘车记录 195 次”

### 当前不能直接坐实的字段

#### 工资收入 `218.27` 万元

- 原始银行流水中可直接以“月工资”类交易识别出的金额约为 `192.1430` 万元
- 如果放宽到 `中国科学院过程工程研究所` 全部相关入账，也只有 `203.8458` 万元
- 国税原始文件只有 `应纳税额`，不能直接反推出 HTML 中的工资金额

结论：

- `218.27` 万元目前缺少直接原始证据支撑

### 当前判断

- `真实收入 275.96` / `真实支出 168.90` 这两个最终值，可能并非完全错误
- 但 HTML 展示层对“原始口径”和“剔除项”的拆解存在明显错位
- 典型表现是：一笔 `40 万级本人自转` 在“原始统计”和“剔除项”中同时漏记，导致中间展示不自洽，但最终净值还能凑平

### 当前阶段性结论

- `朱永平` 这一节不是“结论全错”
- 但它已经足以证明：HTML 展示层仍然不能视为完全可信
- 当前最可疑的问题集中在：
  - 原始总流入 / 总流出展示口径
  - 现金收支展示口径
  - 大额收支统计口径
  - 工资展示口径
  - 铁路记录计数口径

---

## 样本二

### 抽样对象

- 对象：朱明
- HTML 章节位置：`初查报告.html` 第四部分朱明家庭，个人章节起始于 `L7861`

### 已直接核实一致的字段

- 银行原始总流入 / 总流出：
  - HTML：`228.46 / 224.82` 万元
  - 原始银行流水直接汇总：`228.4563 / 224.8155` 万元
- 大额收入 / 大额支出：
  - HTML：`41` 笔 `184.87` 万元 / `37` 笔 `189.81` 万元
  - 原始银行按单笔 `>=1万元` 直接复算：`41` 笔 `184.8741` 万元 / `37` 笔 `189.8080` 万元
- 房产主体信息：
  - 名下房产 `1` 处，坐落 `奉贤区吴塘路988弄32号1604`
  - 建筑面积 `95.00㎡`
  - 登记时间 `2024-02-01`
  - 共有人 `吴嘉欣`
- 同住址 `3` 人
- 航班记录 `6` 条
- 铁路记录 `350` 条
  - 其中票面信息 `163` 条，交易信息 `187` 条
- 住宿记录 `15` 条
- 电子钱包：
  - 财付通交易 `122` 笔
  - 微信登录轨迹 `511` 条
- 理财产品情况：
  - 原始理财文件未见非空“金融理财名称”或持仓金额字段
  - HTML 写“未查见名下理财产品情况”，这一点当前可基本成立

### 已直接核实不一致的字段

#### 1. 年度资金时序

- HTML：
  - 2022：`33.39 / 31.72`
  - 2023：`33.94 / 32.24`
  - 2024：`69.89 / 66.40`
  - 2025：`15.67 / 14.89`
- 原始银行流水按年度直接汇总：
  - 2022：`50.8332 / 45.0809`
  - 2023：`36.2736 / 30.6725`
  - 2024：`122.2105 / 115.1901`
  - 2025：`19.1390 / 33.8720`

结论：

- HTML 的“年度资金时序”不是原始银行年度收支
- 它与工资年度表几乎同形，属于展示字段绑错口径

#### 2. 工资收入

- HTML：
  - 工资收入 `152.89` 万元
- 原始银行流水人工工资口径复算：
  - 合计 `155.1834` 万元
  - 2022：`33.6813`
  - 2023：`34.9668`
  - 2024：`70.6108`
  - 2025：`15.9244`

结论：

- HTML 工资组件与原始银行工资口径不一致

#### 3. 现金收支

- HTML：
  - 现金存入 `0`
  - 现金取出 `0`
- 原始银行流水人工口径复算：
  - 现金存入 `2.20` 万元，`3` 笔
  - 现金取现 `2.20` 万元，`4` 笔

结论：

- HTML 现金组件漏识别，不是个例

#### 4. 账户余额

- HTML：
  - 银行卡余额 `126692.18` 元
- 账户信息台账余额：
  - 交行 `81506.43`
  - 工行 `49562.68`
  - 合计 `131069.11`
- 流水末笔余额：
  - 交行 `77129.50`
  - 工行 `49562.68`
  - 合计 `126692.18`

结论：

- HTML 余额表取的是“流水末笔余额”，不是“账户信息台账余额”

#### 5. 房产交易金额

- HTML：
  - 房产表中“交易金额”写 `暂无数据`
  - 购房分析段写“房产总交易价格信息缺失”
- 原始房产文件：
  - 预告登记交易金额 `3475463.00` 元
  - 不动产权证交易金额 `3471808.00` 元
- 原始国税文件：
  - 存在 `契税` `1` 条

结论：

- 房产并非“交易金额缺失”
- HTML 房产金额展示与购房分析段均失真

#### 6. 税务表述

- HTML：
  - “本人仅缴纳个人所得税，无其他税款缴纳记录”
- 原始国税文件：
  - `个人所得税` `94` 条
  - `印花税` `1` 条
  - `契税` `1` 条

结论：

- HTML 税务结论错误，至少漏掉了印花税与契税

### 当前判断

- `朱明` 这一节说明：HTML 不是“所有组件都错”
- 原始总流入 / 总流出、大额收支、同住址、住宿、航班、铁路这些字段可以落地
- 但年度资金时序、工资、现金、余额、房产金额、税务结论存在明确展示错位或漏写

### 当前阶段性结论

- 第二轮样本进一步证明：
  - `年度资金时序` 存在系统性绑错字段的问题
  - `现金组件` 存在系统性漏识别问题
  - `账户余额` 稳定采用了错误口径
- 同时也证明：
  - `总流入 / 总流出`、`大额收支` 不是全局统一错误，更像对象级规则不稳定

---

## 问题补记：马尚德 2025 年工资历史差额

### 已定位的历史差额

- 用户此前指出：
  - 2022-2024 与原始核对一致
  - 2025 年工资展示少了约 `9207` 元

### 原始证据

- 马尚德原始银行流水中，存在两笔“对手方缺失 + 摘要正常”，但交易类型明确为 `工资` 的入账：
  - `2025-05-25 03:32:30`，`4500.00` 元
  - `2025-06-25 03:48:26`，`4704.82` 元
- 两笔合计 `9204.82` 元，与此前盲审指出的 `9207` 元差额基本一致。

### 根因判断

- 这不是原始数据问题，而是旧工资识别逻辑过度依赖 `摘要/对手方`，没有把 `交易类型=工资` 作为补充证据。

### 当前状态

- 本轮代码已覆盖该场景，并补了回归测试：
  - `tests/test_financial_profiler.py::test_calculate_yearly_salary_uses_transaction_type_when_counterparty_missing`
- 也就是说，`马尚德 2025 年少 9207 元` 这一条，根因已经坐实，且当前代码链路已补回。

---

## 样本三：王永健

### 抽样对象

- 对象：王永健
- HTML 章节位置：`核查结果分析报告.html` 第五部分王永健，个人章节起始于 `五、王永健资产收入情况及数据分析`

### 已直接核实一致的字段

- 银行原始总流入 / 总流出：
  - 原始银行流水按 HTML 当前展示口径复核，可落到 `155.33 / 139.02` 万元附近
  - 但注意：这并不是“原始流水直接求和”口径，见下文不一致项
- 年度资金时序：
  - HTML `2022-2026` 各年收支可从原始银行流水直接按年求和落地
- 现金支出：
  - 原始文件可直接落到 `3` 笔取现，合计 `0.50` 万元
- 税务：
  - 原始国税文件 `174` 条，合计 `263095.39` 元
  - 税种包含 `个人所得税 / 车辆购置税 / 契税 / 印花税`
- 保险：
  - 原始保险保单 `236` 条，累计保费 `390674.61` 元

### 已直接核实不一致的字段

#### 1. “原始总流入 / 总流出”并非原始流水直接求和

- HTML：
  - 总流入 `155.33` 万元
  - 总流出 `139.02` 万元
- 原始银行流水直接按 `进/出` 求和：
  - 总流入 `175.3281` 万元
  - 总流出 `159.4010` 万元

结论：

- HTML 的“原始口径”已经混入了部分内部划转剔除，不能再叫“原始流水直接汇总”。

#### 2. 大额转账汇总和明细表口径打架

- 原始银行流水按单笔 `>= 1 万元` 直接复算：
  - 大额流入 `54` 笔，`131.04` 万元
  - 大额流出 `16` 笔，`87.04` 万元
- HTML 汇总写的是：
  - 大额流入 `53` 笔，`111.04` 万元
  - 大额流出 `15` 笔，`66.72` 万元
- 但 HTML 明细表里又仍然展示了这些被汇总口径排除的记录：
  - `2024-10-04 09:37:55` 现金存款 `20.00` 万元
  - `2025-10-05 14:21:49/14:23:49` 王永健与王永健之间的 `20` 万级内部划转

结论：

- 王永健这一节再次证明：HTML 汇总和 HTML 明细不是同一套口径。

#### 3. 工资被单位业务款抬高

- HTML：
  - 2025 年工资 `14.83` 万元
  - 2022-2026 合计工资 `59.11` 万元
- 原始流水严格工资口径复核：
  - 来自 `北京中科创嘉人力资源咨询有限公司` 的明确工资，2025 年合计 `14.0642` 万元
- 被 HTML/程序旧口径一并吞进工资的 2025 年非工资入账：
  - `2025-04-01` `3865.00` 元 `中国科学院过程工程研究所` `购买真空封装袋`
  - `2025-05-09` `640.00` 元 `中国科学院过程工程研究所` `购买标签打印机进纸器`
  - `2025-05-09` `3185.00` 元 `中国科学院过程工程研究所` `购买标签打印机进纸器`
- 三笔合计 `7690.00` 元。

结论：

- 这不是人工复核误差，而是工资识别规则把“已知发薪单位”的采购/业务回款也计入了工资。

### 代码根因

- 代码根因已定位到：
  - `financial_profiler.py` 的 `_identify_salary_by_whitelist()`
  - `financial_profiler.py` 的 `_identify_business_reimbursement_income()`
- 旧逻辑问题：
  - 只要对手方命中“已知发薪单位白名单”，就容易整批吞并为工资
  - `购买/采购/材料/维修/邮寄费` 这类单位业务款关键词没有被报销识别链覆盖

### 本轮修复

- 已补强：
  - 单位业务往来款识别新增 `采购 / 购买 / 购置 / 材料 / 耗材 / 设备 / 维修 / 邮寄费 / 运费`
  - 工资白名单新增业务关键词保护，避免同一单位的采购回款再次误计为工资
- 已补回归测试：
  - `tests/test_financial_profiler.py::test_calculate_yearly_salary_excludes_procurement_reimbursement_from_known_payer`

### 当前阶段性结论

- 王永健样本把问题本质坐实了：
  - 不是 HTML 模板单纯写错字
  - 是“工资白名单过宽 + 业务报销识别不足”共同导致工资口径被抬高
- 同时也再次证明：
  - HTML 中标成“原始口径”的数字，仍有一部分已经提前混入过滤逻辑

## 2026-03-22 11:53 二次复核补记

### 新发现：HTML 展示层仍存在工资口径命名混用

- 修复工资识别后重新全量生成 HTML，发现同一人员页面仍同时出现两套工资数字：
  - 王永健：
    - 严格年度工资 `55.04` 万元
    - 资金流入叙述写成“工资收入 `56.15` 万元”
  - 马尚德：
    - 严格年度工资 `141.35` 万元
    - 资金流入叙述写成“工资收入 `141.37` 万元”
- 继续追代码确认后，问题不在清洗层，而在 `investigation_report_builder.py` 的展示层：
  - 年度工资段落取的是 `yearly_salary.summary.total`，属于严格工资口径
  - 部分叙述段落取的是 `_get_salary_metrics()`，属于综合工资口径
  - 但旧模板把两者都写成了“工资收入”，导致业务阅读时误以为前后数字打架

### 本次修复

- 已将所有使用 `_get_salary_metrics()` 的对外文案统一改名为“工资性收入 / 非工资性收入”
- 严格年度工资段落继续保留“工资收入”表述
- 同步修正 `config/report_phrases.yaml` 中 `fund_flow_analysis.unknown_income.normal` 的正常话术

### 修复后验证

- 新 HTML 已重生到：
  - `output_reaudit_20260322_1142/analysis_results/核查结果分析报告.html`
- 关键回归点：
  - 王永健：
    - 年度工资段落仍为 `55.04` 万元
    - 资金流入叙述已改为“工资性收入 `56.15` 万元”
  - 马尚德：
    - 年度工资段落仍为 `141.35` 万元
    - 资金流入叙述已改为“工资性收入 `141.37` 万元”

### 结论修正

- 到这一轮为止，工资相关问题已经拆成两个层面：
  - 数据层问题：王永健采购回款误入工资，已在 `financial_profiler.py` 修正
  - 展示层问题：严格工资与综合工资命名混用，已在 `investigation_report_builder.py` 与短语配置修正

## 2026-03-22 12:15 朱明盲审补记

### 抽样方式

- 随机种子：`20260322`
- 抽中对象：`朱明`
- 目标 HTML：
  - `output_reaudit_20260322_1142/analysis_results/核查结果分析报告.html`

### 不依赖程序逻辑的原始核验结论

- 银行流水原始汇总：
  - 流入 `2284562.66` 元
  - 流出 `2248154.54` 元
  - 与 HTML `228.46 / 224.82 万` 对上
- 现金：
  - 收现 `22000.00` 元
  - 取现 `38000.00` 元
  - 与 HTML `2.20 / 3.80 万` 对上
- 微信登录轨迹：
  - 原始 `WX登录轨迹（公开）.txt` 可落到 `511` 条有效时间记录
  - 最近登录时间 `2026-02-05 13:48:30`
  - 与 HTML 对上
- 工资：
  - 严格年度工资合计 `1528869.41` 元
  - 2022-2025 年度拆分与 HTML 完全对上
  - 本轮未发现新的工资识别错误

### 本轮命中的确定性 Bug

- Bug 现象：
  - 旧 HTML 将朱明“银行存款情况”写成 `180631.79` 元、`6` 个账户
- 原始账户信息文件复核：
  - `银行业金融机构账户信息（定向查询）/朱明_..._商业银行账户信息_3.xlsx`
  - 交行 `6222620110059564063` 官方余额 `81506.43` 元
  - 工行流水账户 `10010566012184939` 与官方账户 `1001056601218493945` 实为同一底层账户
  - 工行官方余额 `49562.68` 元
- 正确展示值：
  - `131069.11` 元
  - `5` 个账户

### 根因定位

- 文件：
  - `investigation_report_builder.py`
- 函数：
  - `_build_bank_accounts()`
- 旧逻辑缺陷：
  - 仅按“账号完全相等”匹配流水账户与官方账户
  - 未处理同一银行下“流水账号截断 / 官方账号扩展 / 卡号补充”的等价场景
  - 导致工行账户被同时展示两次，并把 `49562.68` 元重复计入

### 修复结果

- 已补充：
  - 银行名称归一
  - 账号数字归一
  - 同银行下前后缀等价账号识别
  - 追加官方账户前的等价去重
- 已重生 HTML：
  - `output_reaudit_20260322_1142/analysis_results/核查结果分析报告.html`
- 回归结果：
  - 朱明章节已修正为 `131069.11` 元、`5` 个账户
  - HTML 表格中不再同时出现 `10010566012184939` 与 `1001056601218493945`
  - 额外对当前所有个人执行同类重复扫描，未再发现残余重复账户展示

## 2026-03-22 12:36 于巧云盲审补记

### 抽样方式

- 随机种子：`20260323`
- 抽中对象：`于巧云`
- 本轮旧报告：
  - `output_reaudit_20260322_1142/analysis_results/核查结果分析报告.html`
- 本轮修复后新报告：
  - `output_reaudit_20260322_1232/analysis_results/核查结果分析报告.html`

### 不依赖程序逻辑的原始核验结论

- 原始 5 份银行流水直和：
  - 有效记录 `1421` 行
  - 总流入 `2223905.41` 元
  - 总流出 `1914495.75` 元
- 旧 `profiles / HTML` 错误展示为：
  - 总流入 `1893896.34` 元
  - 总流出 `1864480.75` 元
- 差额：
  - 流入少计 `330009.07` 元
  - 流出少计 `50015.00` 元
- 原始钱包与外部资料复核仍成立：
  - 微信登录轨迹 `836` 条，最近登录 `2026-02-05 14:03:03`
  - 财付通交易 `330` 笔
  - 年度工资 `2022-2026` 分别为 `4.24 / 9.99 / 18.02 / 17.13 / 1.55` 万元
  - 银行账户余额 `62205.25` 元、`7` 个账户

### 本轮命中的确定性 Bug

- Bug 现象：
  - 旧清洗产物 `output_reaudit_20260322_1142/cleaned_data/个人/于巧云_合并流水.xlsx` 只有 `1414` 行
  - 被错误吃掉的不是脏数据，而是同一流水号下的合法双分录/手续费分录
- 已抓到的典型误删样本：
  - `ECT0001023513138`：同一流水号下一进一出 `220000.00`
  - `ECT0001J70340000`：同一流水号下一进一出 `110000.00`
  - `B443001005194044`：同一流水号下一进一出 `50000.00 / 50162.71`
  - `53576023`：主交易 `30000.00` + 手续费 `15.00`
  - `458113123`：同一流水号下不同日期、不同金额的两笔利息入账

### 根因定位

- 文件：
  - `data_cleaner.py`
- 函数：
  - `deduplicate_transactions()`
- 旧逻辑缺陷：
  - 对有 `transaction_id` 的记录，旧实现只按“来源文件 + 流水号”做 `drop_duplicates`
  - 这会把同一流水号下的合法双分录、手续费、利息补记、占位流水号复用记录误判成重复
  - 结果是清洗层直接少行，后续 `profiles.json` 与 HTML 全部跟着少算

### 本次修复

- 已将流水号去重键收紧为：
  - `来源文件 + transaction_id + direction + rounded_amount`
  - 若存在账号列，再追加 `account_number`
- 修复目标：
  - 只删除“完全重复行”
  - 保留同一流水号下不同方向、不同金额、不同账户的合法记录
- 已补回归测试：
  - 同流水号主交易 + 手续费并存
  - 同流水号一进一出双分录并存
  - 占位流水号 `-` 的不同金额记录并存

### 修复后验证

- 最小清洗链验证：
  - 新 `clean_and_merge_files()` 结果恢复为 `1419` 行
  - 新总流入 `2223905.41` 元
  - 新总流出 `1914495.75` 元
  - 与原始直和完全对上
- 全量重跑输出：
  - `output_reaudit_20260322_1232`
- 新 `profiles.json` 回归结果：
  - `totalIncome = 2223905.41`
  - `totalExpense = 1914495.75`
  - `transactionCount = 1419`
- 新 HTML 回归结果：
  - 章节：`八、于巧云资产收入情况及数据分析`
  - 资金分析概览已更新为：
    - 总流入 `222.39` 万元
    - 总流出 `191.45` 万元
    - 真实收入 `132.43` 万元
  - “家庭存款与家庭收入匹配分析”段落已更新为：
    - 原始流入 `222.39` 万元
    - 原始流出 `191.45` 万元
    - 真实收入 `132.43` 万元
    - 真实支出 `106.25` 万元
  - “工资奖金收入”与“银行存款情况”仍保持正确：
    - 工资 `50.94` 万元
    - 银行余额 `62205.25` 元、`7` 个账户

### 结论

- 本轮问题已证实为清洗层确定性 Bug，不是 HTML 模板展示误差。
- 修复后，于巧云从原始流水到 `cleaned_data`、`profiles.json`、HTML 成稿已重新打通并对齐。

## 2026-03-22 12:50 全局影响评估补记

### 旧输出 vs 新输出差分

- 对比目录：
  - 旧：`output_reaudit_20260322_1142`
  - 新：`output_reaudit_20260322_1232`
- 受本次去重修复影响的主体，不止于巧云一人：
  - 吴嘉欣：`+18` 行，流入 `+710671.84`，流出 `+1203227.08`
  - 朱永平：`+29` 行，流入 `+402978.89`，流出 `+400000.00`
  - 王永健：`+14` 行，流入 `+200005.31`，流出 `+203785.28`
  - 赵峰：`+8` 行，流入 `+211666.93`，流出 `+200000.00`
  - 金琳：`+50` 行，流入 `+867733.81`，流出 `+202193.25`
  - 陈斌：`+14` 行，流入 `+165.33`，流出 `+40000.00`
  - 马尚德：`+14` 行，流入 `+1178.57`
  - 贵州锐晶科技有限公司：`+324` 行，流出 `+10495798.38`

### 吴嘉欣抽样复证

- 新增 `18` 行全部能追到原始文件：
  - `中国银行交易流水.xlsx`
    - `348900058`：同流水号下同时存在 `503227.08` 转出、`503227.08` 转入，以及 `3227.08` 入账
    - `212312263`：同流水号下同时存在 `500000.00` 转入、`500000.00` 转出
    - `9291056`：同流水号下同时存在 `203600.00` 转入、`203600.00` 转出，以及 `3600.00` 入账
    - `221434450`：同流水号下同时存在 `200000.00` 转入、`200000.00` 转出
  - `中国建设银行交易流水.xlsx`
    - 12 笔 `利息存入` 原始记录均真实存在，旧版仅因占位流水号 `-` 被误压重
- 结论：
  - 吴嘉欣的增量不是新引入噪音，而是旧版去重误删的合法原始流水

### 贵州锐晶科技有限公司抽样复证

- 新增 `324` 行全部来自原始 `中国银行交易流水.xlsx`
- 抽查最大几笔均为“主交易 + 手续费”同流水号双分录：
  - `122405670`
    - `1600000.00` 转出给李智勇
    - `32.00` 对公跨行柜台转账汇款手续费
  - `81818349`
    - `650000.00` 转出给北京智晟睿科技有限公司
    - `20.00` 对公跨行转账汇款手续费
  - `113059749`
    - `500000.00` 转出给北京智晟睿科技有限公司
    - `15.00` 对公跨行转账汇款手续费
  - `118734011`
    - `383203.68` 转出给遵义自贸协同物业管理有限公司
    - `15.00` 对公跨行柜台转账汇款手续费
- 结论：
  - 公司侧也被同一类去重 Bug 命中，且原始证据链与个人样本一致

### 当前信心边界

- 现在可以确认：
  - 本轮修复打中的是真实全局根因，不是单点特例
  - 该 Bug 同时影响个人与公司主体
  - 新输出较旧输出的主要增量，已被两个独立样本的原始流水复证
- 仍需继续盲审的方向：
  - 对受影响金额较大的 `金琳 / 吴嘉欣 / 贵州锐晶科技有限公司` 继续扩样，可进一步压缩残余不确定性

### 全体受影响主体分桶补记

- 对本次受影响的 `9` 个主体，已将“旧输出缺失、新输出恢复”的全部增量行按结构特征重新分桶。
- 分桶口径：
  - `placeholder_interest`：占位流水号 `-` 的利息入账
  - `txid_opposite_direction_pair`：同流水号下一进一出双分录
  - `txid_fee_pair`：同流水号下主交易 + 手续费
  - `txid_multi_split`：同流水号下合法多分录
- 分桶结果：
  - 于巧云：`5` 行，`txid_multi_split:2`，`txid_opposite_direction_pair:3`
  - 吴嘉欣：`18` 行，`placeholder_interest:12`，`txid_opposite_direction_pair:6`
  - 朱永平：`29` 行，`placeholder_interest:26`，`txid_opposite_direction_pair:3`
  - 王永健：`14` 行，`placeholder_interest:9`，`txid_multi_split:2`，`txid_opposite_direction_pair:3`
  - 赵峰：`8` 行，`placeholder_interest:3`，`txid_multi_split:2`，`txid_opposite_direction_pair:3`
  - 金琳：`50` 行，`txid_multi_split:1`，`txid_opposite_direction_pair:49`
  - 陈斌：`14` 行，`placeholder_interest:12`，`txid_opposite_direction_pair:2`
  - 马尚德：`14` 行，`placeholder_interest:14`
  - 贵州锐晶科技有限公司：`324` 行，`txid_fee_pair:324`
- 结论：
  - `9/9` 受影响主体的新增行全部可以被同一条误去重根因解释
  - 本轮分类未出现 `unresolved`
  - 截至本次补记，尚未发现第二类独立代码根因

## 2026-03-22 14:10 金琳扩样盲审补记

### 新输出 vs 原始引用行

- 新输出口径：
  - `transactionCount = 6346`
  - `totalIncome = 10405290.36`
  - `totalExpense = 10057128.33`
  - `realIncome = 3329206.68`
  - `realExpense = 5546169.81`
  - `yearly_salary.total = 852762.27`
- 直接以 `cleaned_data/个人/金琳_合并流水.xlsx` 中的 `来源文件 + source_row_index` 回原始 Excel：
  - `source_row_index` 为 Excel 物理行号，对应原始 DataFrame 索引 `+2`
  - 新输出实际引用原始行 `6346` 条，且 `6346` 组 `(来源文件, source_row_index)` 全部唯一
- 按这些实际引用的原始行、直接使用原始 `借贷标志 + 交易金额` 复算：
  - 总流入 `10405290.36` 元
  - 总流出 `10057128.33` 元
- 结论：
  - 金琳新输出的总笔数、总流入、总流出与其实际引用的原始银行流水完全对齐

### 原始总库复算中的 `181842.55` 差额解释

- 若仅按“来源文件 + 本方账号 + 交易时间 + 流水号 + 借贷标志 + 金额 + 对手 + 摘要”做原始库去重，原始会得到：
  - `6349` 行
  - 总流入 `10587132.91` 元
  - 总流出 `10057128.33` 元
- 与新输出相比，多出的仅是 `3` 条招商银行流入，合计 `181842.55` 元：
  - `ORXTNA2412073100020000012152`，`3000.00` 元
  - `ORXTNA2412073100020000012145`，`104212.55` 元
  - `ORXTNA2308273100020000005518`，`74630.00` 元
- 这 `3` 条并非新发现漏算，而是招商银行原始文件内同 `流水号 + 金额 + 借贷方向` 的镜像重复：
  - 同一 `txid` 在原始文件里各出现 `2` 次
  - 新输出保留的是日期与 `txid` 编码一致的那条：
    - `ORXTNA241207...` 保留 `2024-12-07`，舍弃 `2024-11-22`
    - `ORXTNA230827...` 保留 `2023-08-27`，舍弃 `2023-08-22`
- 结论：
  - 当前未证实这 `181842.55` 属于应纳入的新合法流水
  - 更高概率是招商银行原始导出中的同流水号镜像重复，不构成第二类独立代码根因

### 新增 `50` 行扩样复证

- 新旧输出差分：
  - 新增 `50` 行
  - 流入增量 `867733.81` 元
  - 流出增量 `202193.25` 元
- 结构分桶结论：
  - `txid_opposite_direction_pair: 49`
  - `txid_multi_split: 1`
- 代表性原始样本：
  - 中国银行 `126822236`
    - 同一时点、同一流水号下同时存在：
      - `excel_row 193`：`进 100000.00`，对手 `金琳`
      - `excel_row 194`：`出 100000.00`，对手 `金琳`
  - 上海银行 `FT25074686649408`
    - `excel_row 120`：`出 100000.00`，对手 `金琳`，摘要 `存款帐户开户`
    - `excel_row 141`：`进 100000.00`，对手 `金琳`，摘要 `存款帐户开户`
  - 上海银行 `FT25074810323052`
    - `excel_row 119`：`出 100000.00`
    - `excel_row 140`：`进 100000.00`
  - 兴业银行 `202408122165329210046`
    - `excel_row 10`：`出 45000.00`，摘要 `活期转定期`
    - `excel_row 153`：`进 45000.00`，摘要 `活期转定期`
  - 广发银行占位流水号 `**`
    - 同一占位 `txid` 下存在多笔不同日期、不同金额记录
    - 新增恢复的 `3` 笔合计 `189.32` 元，属于占位流水号复用场景，说明旧版按 `txid` 粗暴去重会误删合法记录

### 金琳结论

- 金琳这一轮扩样盲审后，可以确认：
  - 新输出展示的总笔数、总流入、总流出与原始引用行一致
  - 新增 `50` 行仍全部落在本轮已修复的误去重根因范围内
  - 目前未从金琳样本中发现第二类独立代码 Bug
