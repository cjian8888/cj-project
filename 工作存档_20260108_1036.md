# 工作存档 - 2026年1月8日 10:36

## 今日主要工作

### 1. 房产数据重复问题修复

**问题描述**：

- 使用新数据集（包含三次查询）测试时，发现报告中房产数据严重重复
- 滕雳、施灵、施承天都显示16套房产（实际应为6-7套去重后）
- 同一房产因夫妻共有被重复计算

**修复方案**：

#### 1.1 `asset_analyzer.py` - `extract_properties()` 函数

- 添加房产去重逻辑
- 使用 `(房地坐落, 登记时间[:10], 建筑面积)` 作为唯一键
- 同一房产多次出现时，合并共有人信息而非重复添加

```python
# 用于去重的字典，key为(房地坐落, 登记时间字符串, 建筑面积)
property_dedup = {}
# ...
dedup_key = (address, date_key, area)
if dedup_key in property_dedup:
    # 已存在，将当前产权人添加为共有人
    existing_prop['共有人名称'] = f"{existing_co_owners}, {person}"
    existing_prop['共有情况'] = '共同共有'
```

#### 1.2 `asset_analyzer.py` - `calculate_family_assets()` 函数

- 添加全局资产分配跟踪
- 避免同一房产被多个核心人员重复统计
- 优先将房产分配给产权人本人（如果是核心人员）

```python
assigned_properties = set()  # 全局跟踪已分配的房产
# 优先规则：如果产权人是当前核心人员本人
if owner == person:
    family_properties.append(prop)
    assigned_properties.add(prop_key)
```

### 2. 过程数据清单文件去重

**问题描述**：

- 报告中"过程数据清单"出现重复文件名

**修复方案**：

- 在 `report_generator.py` 中使用 `set()` 去重后排序

### 3. 人员/公司分类错误修复

**问题描述**：

- 公司被错误识别为"核心人员"
- 原因：统一社会信用代码 `913100006076928753` 匹配了身份证号正则表达式

**修复方案**（`file_categorizer.py`）：

1. 增强身份证号验证：添加省份代码校验（11-65有效范围）
2. 增强统一社会信用代码验证：
   - 先检查身份证号（更严格）
   - USCC需满足：前两位是登记管理部门代码(91/92/93)，或包含字母

```python
valid_province_codes = ['11', '12', '13', '14', '15', '21', '22', '23', 
                        '31', '32', '33', '35', '36', '37', ...]

def is_valid_id_card(code):
    if not id_card_pattern.match(code):
        return False
    return code[:2] in valid_province_codes

def is_valid_uscc(code):
    # 如果是纯数字且前两位是有效省份代码，则是身份证号
    if code.isdigit() and code[:2] in valid_province_codes:
        return False
    # 前两位是91/92/93等登记管理部门代码
    uscc_dept_codes = ['11', '12', '13', '91', '92', '93', ...]
    ...
```

## 修复效果对比

| 项目 | 修复前 | 修复后 |
|------|--------|--------|
| 核心人员 | 7人（错误包含3家公司） | 4人 ✅ |
| 涉案公司 | 0家 | 3家 ✅ |
| 滕雳房产 | 16套 | 1套 ✅ |
| 施灵房产 | 16套 | 4套 ✅ |
| 施承天房产 | 16套 | 0套 ✅ |
| 施育房产 | 1套 | 1套 ✅ |

## 涉及文件变更

1. `asset_analyzer.py` - 房产提取和家族资产计算去重
2. `report_generator.py` - 过程数据清单文件去重
3. `file_categorizer.py` - 身份证号/统一社会信用代码识别增强

## 待处理事项

- [ ] 理财产品数据处理优化
- [ ] 金额统计逻辑优化（区分资金流入/流出 vs 收入/支出）
- [ ] 理财产品全貌性报告生成
